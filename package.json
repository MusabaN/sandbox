{
  "name": "sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/cli.ts --compile --outfile dist/sandbox",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
